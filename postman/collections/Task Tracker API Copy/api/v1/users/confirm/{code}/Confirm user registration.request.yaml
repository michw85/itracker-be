$kind: http-request
description: Confirms a user's registration using a confirmation code sent via email.
url: "{{baseUrl}}/api/v1/users/confirm/:code"
method: GET
headers:
  Accept: application/json
pathVariables:
  code: string
examples: ./.resources/Confirm user registration.resources/examples
order: 1000
